fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pandas>=2.2.0
openpyxl>=3.1.2
xlrd>=2.0.1            # legacy .xls reads (xlrd 2.x reads .xls only)
python-docx>=1.1.0     # DOCX ingestion and mutation for the workspace layer
pytesseract>=0.3.10    # OCR Python wrapper (requires system Tesseract binary; see README)
opencv-python-headless>=4.10.0  # image preprocessing (denoise / threshold / sharpen)
pymupdf>=1.24.0        # PDF page rendering + text extraction (no MuPDF system install needed)
Pillow>=10.0.0         # image loading; explicit here even though transitive via matplotlib
numpy>=1.26.0
httpx>=0.27.0
pydantic>=2.7.0
langgraph>=0.2.0
matplotlib>=3.8.0
python-multipart>=0.0.9
reportlab>=4.0.0
ddgs>=9.0.0
chardet>=5.2.0         # primary CSV/TSV encoding detector (charset_normalizer fallback)
